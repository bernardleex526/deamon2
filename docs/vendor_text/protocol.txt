basic_server 通信协议总览
本文档介绍山猫 M20 由 basic_server 服务向外提供的通讯协议。 
完整协议定义和具体指令格式查询请参考
软件开发指南
。
前置阅读：
对外通信方式
。
目录
1. 协议总则
1.1 协议层次
1.2 接入通道
1.3 交互机制
1.4 应用协议数据单元
1.5 协议头部结构
1.6 ASDU结构
2. 接口字典
3. 心跳与连接保活
3.1 心跳机制
3.2 主动上报与心跳来源
4. 通讯错误码机制
﻿
﻿
1.
协议总则
1.1 协议概述
本协议基于TCP或UDP协议（根据具体开发需要进行选择），适用于机器人本体与上位机（外部板卡或系统）通信使用。
﻿
1.2 接入通道
本协议使用时，机器人本体为TCP/UDP服务端，外部板卡或系统为TCP/UDP客户端。协议的UDP服务端地址与端口为10.21.31.103:30000，TCP服务端为10.21.31.103:30001。
协议
地址
端口
长连接
推荐场景
UDP
10.21.31.103
30000
否
高频速度指令（≥20Hz）、低延迟优先
TCP
10.21.31.103
30001
是
任务下发、状态订阅、可靠传输
1.3 交互机制
如下图所示，本协议主要使用请求/响应机制。上位机可主动向机器人本体发起数据或状态查询请求，机器人本体通过响应回复部分请求。
﻿
﻿
﻿
1.4 应用协议数据单元
本协议APDU（应用协议数据单元）采用"协议头部+ASDU（应用服务数据单元）"结构，每个APDU可以携带1个ASDU。
﻿
﻿
﻿
1.5 协议头部结构
协议头部长度固定16字节，内容如下表所示。
序号
内容
长度
值
备注
1
同步字符
1
0xeb
固定
2
同步字符
1
0x91
固定
3
同步字符
1
0xeb
固定
4
同步字符
1
0x90
固定
5
长度
2
﻿
APDU中ASDU字节段的长度，采用小端字节序，低字节在前，ASDU最大长度限制为65535字节
6
报文ID
2
﻿
每帧报文的唯一标识，用于识别请求帧和响应帧的对应关系，值由请求方控制，响应帧将采用相同值回复；
从0开始递增，达到65535后重新从0开始；
采用小端字节序，低字节在前
7
ASDU格式
1
﻿
ASDU数据格式类型判断位；
ASDU数据内容采用XML格式时，值为 0x00；
ASDU数据内容采用JSON格式时，值为 0x01
8
预留
7
0x00
预留7字节，暂填0
推荐使用 JSON 格式
，JSON 格式下 ASDU 处理性能更快且数据结构更全面，支持更多消息类型。 XML 仅在与既有系统对接、或客户内部要求时使用。
﻿
1.6 ASDU结构
本协议ASDU数据内容采用JSON或XML格式组织（参考"
1.1.5
 
协议头部结构
"部分，需在协议头部中写明），包含以下通用字段：
字段
含义
Type
消息类型
Command
消息命令码
Time
报文发送时间，本地时区，格式：YYYY-MM-DD HH:MM:SS
Items
消息所携带的参数项
一个JSON的ASDU案例如下：
10
{
	"PatrolDevice": {
		"Type": 1002,
		"Command": 1,
		"Time": "2023-01-01 00:00:00",
		"Items": {
		}
	}
}
一个XML的ASDU案例如下：
8
<?xml version="1.0" encoding="UTF-8"?>
<PatrolDevice>  
	<Type>1002</Type>
	<Command>1</Command>  
	<Time>2023-01-01 00:00:00</Time>
	<Items/>  
</PatrolDevice>
﻿
﻿
2.
接口字典
Type
对应子模块
典型 Command
2
运动控制
21=运动控制轴指令, 22=运动状态, 23=步态切换, 24=自主充电
100
连接状态保活
100=心跳指令(1Hz)
1002
机器人状态
（机器人主动上报）
3=错误列表(2Hz), 4=运动状态(10Hz), 5=系统状态(2Hz), 6=基础状态(2Hz)
1003
导航任务
1=单点导航, 2=路径导航, 3=取消任务, 4=任务状态查询
1101
设备使能
2=照明灯, 5=模式切换, 6=休眠设置, 7=休眠配置查询
文档中出现的所有
 Type / Command 
均为
十进制数字
。
﻿
﻿
3.
心跳与连接保活
3.1 心跳机制
●
无论使用UDP或TCP，客户端均应以不低于1Hz的频率发送任意指令作为心跳（建议使用特定的心跳指令
Type=100, Command=100
）；服务端超过 2s未收到客户端任何请求则认为客户端离线，停止向该客户端推送所有主动上报数据。
●
使用
UDP
下发运动控制轴指令或速度指令时，建议发送频率不低于20Hz；服务端检测到客户端断开时会自动停止该客户端的速度指令转发，机器人进入安全状态。
●
建议客户端超过3秒未收到服务端任何响应消息时判定服务端断连。
﻿
3.2 主动上报与心跳来源
﻿
basic_server
以
心跳来源的 IP/端口
作为目标地址，向客户端主动推送以下数据：
Type
Command
周期
内容
1002
3
2Hz / 异常事件即时上报一次
异常状态
1002
4
10Hz
运控状态：姿态、速度、16关节
1002
5
2Hz
设备状态：电池、温度、设备、GPS
1002
6
2Hz
基础状态：运动状态、充电状态等
订阅方式：
●
TCP
：建立连接后自动接收上述所有主动上报
●
UDP
：需先发送一次对应查询请求（如
Type=1002, Command=5
），服务端记录来源 IP/端口后持续推送该类型数据
﻿
﻿
4.
通讯错误码机制
basic_server 在收到客户端下发的每一个请求后，都会返回该条消息：
JSON 形式：
12
{
  "PatrolDevice": {
    "Type": XXXX,
    "Command": XX,
    "Time": "2023-01-01 00:00:00",
    "Items": {
      "ErrorCode": 0,
      "ErrorMessage": "Success"
    }
  }
}
XML 形式：
11
<?xml version="1.0" encoding="UTF-8"?>
<PatrolDevice>
    <Type>XXXX</Type>
    <Command>XX</Command>
    <Time>2023-01-01 00:00:00</Time>
    <Items>
        <ErrorCode>0</ErrorCode>
        <ErrorMessage>Success</ErrorMessage>
    </Items>
</PatrolDevice>
其中，回复的 Type 和 Command 字段值与客户端下发的对应请求消息中的 Type 和 Command 字段值相同。
Items字段中参数项的值与含义如下（ErrorMessage与ErrorCode一一对应，用作对ErrorCode的补充描述）：
ErrorCode
（十六进制）
ErrorCode
（十进制）
ErrorMessage
含义/建议动作
0x0000
0
Success
发送成功，可继续下一步
0xE001
57345
Unsupported data format
发送的数据格式不支持（非xml和Json）
，建议
检查帧头 Format 字段
0xE002
57346
Data parsing failed
协议格式异常，数据解析失败，缺少PatrolDevice、Items、Type、Command
，建议
校验 JSON/XML 格式
0xE003
57347
Unsupported protocol type
找不到Type、Command对应的协议，建议检查 Type、Command 值
0xE004
57348
Missing necessary fields
缺少协议字段
0xE005
57349
Mismatched data type
字段类型不匹配
，建议
校对字段类型
0xE006
57350
Mismatched request client
请求客户端不匹配，例如：轴指令发送时，要求2s内发送的指令来自同一个客户端
0xE007
57351
No operation permission
无操作权限
0xE008
57352
Not allowed operation
当前状态下不允许的操作
，建议
检查状态机（如未站立不能发速度）
0xE009
57353
Operation failure
操作失败，建议查看 
Type=1002, Cmd=3
 错误详情
0xE00A
57354
Unsupported function
不支持的功能，例如：导航功能只有Pro版本支持，建议确认固件版本
0xE00B
57355
Internal error
内部错误，如内部文件打开失败，内部文件解析失败等
，建议
查看日志
注意
：
ErrorCode=0x0000
 仅表示命令已被接收并触发，不代表业务执行成功。
﻿